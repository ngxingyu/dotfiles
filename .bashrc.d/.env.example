# Template for .bashrc.d/.env (gitignored, per-machine, never committed).
# Copy this file to .env alongside it and fill in real values there.
# KEY=VALUE, one per line. Loaded by both bash and zsh via 60-secrets.sh.
EXAMPLE_API_KEY=
EXAMPLE_TOKEN=
